import { Button } from '@/components/Button';

export const LogoutButton = () => {
  return (
    <Button
      title="Выйти"
      type="button"
      variant="secondary"
      size="small"
    />
  );
};
